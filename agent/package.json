{
  "name": "voicedrop-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "agents": "latest"
  },
  "devDependencies": {
    "better-sqlite3": "^12.11.1",
    "vitest": "^2",
    "wrangler": "^4"
  }
}
